{% extends 'admin/layout_admin.html.twig' %}

{% block title %}Performs{% endblock %}

{% block body %}
    <h1>Performs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ perform.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ perform.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ perform.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ perform.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('performs_index') }}">back to list</a>

    <a href="{{ path('performs_edit', {'id': perform.id}) }}">edit</a>

    {{ include('admin/performs/_delete_form.html.twig') }}
{% endblock %}
